# mri-sync-harnesses

Configurações externas para validação de resources FiveM durante `mri-upstream-sync`.

Este repositório não altera `qbx_core`, `ox_inventory` ou qualquer resource upstream.
Runtime genérico fica em `mri-Qbox-Brasil/workflows`.

Cada configuração declara sources, schema, seed e specs consumidos pelo runner do sync.
